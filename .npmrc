# NPM configuration for EAS builds
# This ensures dependencies install correctly in the build environment

# Use legacy peer dependency resolution for compatibility
legacy-peer-deps=true

# Use exact versions from package-lock.json
save-exact=true

# Timeout for network requests (10 minutes)
fetch-timeout=600000
